run:
  timeout: 5m

linters:
  enable:
    - gosec
    - errcheck
    - staticcheck
    - gocritic
    - govet
    - ineffassign
    - unused

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
