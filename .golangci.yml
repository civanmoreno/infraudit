version: "2"

run:
  timeout: 5m

linters:
  enable:
    - gosec
    - errcheck
    - staticcheck
    - gocritic
    - govet
    - ineffassign
    - unused

formatters:
  enable:
    - gofmt

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
